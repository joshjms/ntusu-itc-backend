packages: 
    yum:
        python3-devel: []
        mariadb-devel: []

container_commands:
    00_nginx_conf:
        command: "sudo cp --force /usr/nginx.conf /etc/nginx/nginx.conf"  
    01_nginx_reload:
        command: "sudo systemctl reload nginx"
